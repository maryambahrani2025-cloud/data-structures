def ins_after(self,x,y):
    if self.head is None:
        print('error0')
        return
    c=self.head
    while c:
     if c.data==x:
         ifc.next is None:
    self.ins_last(y)
    return
a=d node(y)
a.next=c.next
c.next=aa.next.back=a
a.back=c
Return 
c=c.next
print('not Found')